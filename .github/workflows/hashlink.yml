name: HL-Build

on:
  push:
    branches: [ official ]
  pull_request:
    branches: [ official ]
  workflow_dispatch:

jobs:
  HLBuildMatrix:
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest]
        include:
          - os: windows-latest
            artifact_name: FV-HL-Windows
            artifact_path: bin/hl/bin
            haxelib_path: C:/haxelib
            compile_target: hl
            needs_tar: false
            
          - os: ubuntu-latest
            artifact_name: FV-HL-Linux
            artifact_path: FV-HL-Linux.tar.gz
            haxelib_path: ~/haxelib
            compile_target: hl
            needs_tar: true
      fail-fast: false
      
    runs-on: ${{ matrix.os }}
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: krdlab/setup-haxe@master
        with:
          haxe-version: 4.3.6
          
      - uses: HaxeFoundation/setup-hashlink@v1
        with:
          version: latest
      
      # Cache haxelib dependencies
      - name: Cache haxelib dependencies
        uses: actions/cache@v3
        with:
          path: ${{ matrix.haxelib_path }}
          key: ${{ runner.os }}-hl-haxelib-${{ hashFiles('**/*.hxml', '**/haxelib.json', '**/lime.xml') }}
          restore-keys: |
            ${{ runner.os }}-hl-haxelib-
      
      # Install Linux system dependencies (only for Linux)
      - name: Install required Linux libraries
        if: runner.os == 'Linux'
        run: |
          setup/linux.bash
      
      # Setup haxelib directory (Windows)
      - name: Setup haxelib (Windows)
        if: runner.os == 'Windows'
        run: haxelib setup C:/haxelib
        shell: cmd
      
      # Install haxelib dependencies (Windows)
      - name: Install haxelib dependencies (Windows)
        if: runner.os == 'Windows'
        run: |
          haxelib install format --quiet
          haxelib install hxp --quiet
          haxelib git hxcpp https://github.com/SomeGuyWhoLovesCoding/hxcpp-sgwlfnf.git --quiet
          haxelib git lime https://github.com/SomeGuyWhoLovesCoding/lime.git --quiet
          haxelib git peote-view_funkinview https://github.com/SomeGuyWhoLovesCoding/peote-view.git --quiet
          haxelib git linc_luajit_funkinview https://github.com/SomeGuyWhoLovesCoding/linc_luajit_funkinview.git --quiet
          haxelib install input2action --quiet
          haxelib git customtitlebar https://github.com/SomeGuyWhoLovesCoding/customtitlebar.git --quiet
        shell: cmd
      
      # Check version tag
      - name: Check version tag
        run: echo "${{github.run_id}}" > VERSION
        shell: bash
      
      # Compile for Windows
      - name: Compile (Windows)
        if: runner.os == 'Windows'
        run: haxelib run lime build hl --app-version="4.0.0-${{ github.run_id}}"
        shell: cmd
      
      # Compile for Linux
      - name: Compile (Linux)
        if: runner.os == 'Linux'
        run: |
          haxelib run lime rebuild hl
          haxelib run lime build hl --app-version="4.0.0-${{ github.run_id}}"
        shell: bash
      
      # Package Linux build
      - name: Package Linux build
        if: matrix.needs_tar == true
        run: |
          mkdir -p export
          cp -r bin/hl/bin/* export/
          cd export && tar -czvf ../${{ matrix.artifact_name }} *
        shell: bash
      
      # Publish artifact
      - name: Publish artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_path }}
          if-no-files-found: error